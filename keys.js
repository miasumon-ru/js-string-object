

const person = {
    name : 'sadar',
    age : 25,
    profession : 'developer',
    salary : 25000,
    isMarried : true
}

 let keys = Object.keys(person);
//  keys method return an array with the properties of  an object
 console.log(keys);